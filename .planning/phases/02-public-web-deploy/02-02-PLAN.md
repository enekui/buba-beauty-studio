---
phase: 02-public-web-deploy
plan: 02
type: execute
---

<objective>
Publicar `index.html` + `privacy.html` + `js/subscribe.js` + resto de assets a producción y verificar con una suscripción real end-to-end que el formulario crea un contacto OPT_IN en SES.

Purpose: este es el momento en que la plataforma se vuelve "viva" para clientas reales. Si este plan pasa, el negocio ya puede compartir el link y captar suscriptoras.

Output:
- Sitio actualizado en `https://bubabeautystudio.com` con el form funcional.
- Mínimo 1 contacto OPT_IN real en SES Contact List `buba-prod-marketing`.
- Screenshots del flujo completo en `.planning/phases/02-public-web-deploy/e2e-evidence/`.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/references/checkpoints.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-public-web-deploy/02-01-SUMMARY.md
@index.html
@js/subscribe.js
@privacy.html

**Decisiones aplicables:**
- Depende del pipeline de deploy elegido en 02-01.
- SES sandbox puede seguir activo (PR AWS en review); si lo está, la verificación funcionará solo para emails verificados. Documentarlo.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy a producción</name>
  <files>—</files>
  <action>
Ejecutar `bash scripts/deploy-web.sh` (creado en 02-01) y capturar output. Si el script escogido fue AWS, también confirmar que CloudFront invalidation terminó (`aws cloudfront get-invalidation`).

Post-deploy: verificar con `curl` que el HTML servido contiene la URL correcta de API Gateway:

```bash
curl -s https://bubabeautystudio.com | grep -c "BUBA_API_BASE.*execute-api" | grep -q "^1$" && echo "API_BASE OK" || echo "FAIL: placeholder no sustituido"
```
  </action>
  <verify>HTTP 200 en `https://bubabeautystudio.com`, el HTML contiene `window.BUBA_API_BASE` con la URL real (no `__API_BASE__`).</verify>
  <done>Sitio servido con la versión nueva. Deploy idempotente (ejecutar dos veces no rompe nada).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Formulario de suscripción en el footer de bubabeautystudio.com + endpoint de API Gateway protegido con CORS.</what-built>
  <how-to-verify>
Test end-to-end (recomendado desde Safari móvil para simular experiencia real):

1. Abrir `https://bubabeautystudio.com` en móvil o desktop.
2. Scroll hasta el footer. Confirmar que se ve el bloque "Apúntate a nuestras promos" con input email + checkbox consentimiento + botón "Apuntarme".
3. Introducir `adianny+e2e@me.com` (o una dirección propia con sufijo único para no colisionar con tokens anteriores).
4. Marcar el checkbox RGPD.
5. Click "Apuntarme". Debe mostrar mensaje inline "Revisa tu bandeja para confirmar la suscripción." en verde.
6. Abrir bandeja de entrada → debe haber llegado un email con subject "Confirma tu suscripción a Buba Beauty Studio". (Si SES sigue en sandbox: ojo, solo llega a direcciones verificadas → verificar `adianny@me.com` como identity si hace falta, o esperar a salir del sandbox.)
7. Click en el botón "Confirmar suscripción" del email. Debe abrir una página con "¡Listo! Ya estás suscrita a las promos.".
8. Verificar en AWS Console → SES → Contact lists → `buba-prod-marketing` que la nueva dirección aparece como OPT_IN.
9. Screenshots en `.planning/phases/02-public-web-deploy/e2e-evidence/`: (a) form en el footer, (b) confirmación email recibido, (c) landing de confirmación, (d) dirección en contact list.
  </how-to-verify>
  <resume-signal>Escribir "E2E ok" si los 9 pasos pasan, o describir qué falló.</resume-signal>
</task>

</tasks>

<verification>
- [ ] `curl -sI https://bubabeautystudio.com` devuelve 200 y el HTML incluye `BUBA_API_BASE`.
- [ ] `aws sesv2 list-contacts --contact-list-name buba-prod-marketing --filter '{"FilteredStatus":"OPT_IN"}'` devuelve al menos 1 contacto nuevo.
- [ ] Screenshots del e2e en la carpeta de evidencias.
</verification>

<success_criteria>
- Sitio actualizado en producción.
- Mínimo 1 suscripción real completada end-to-end.
- Evidencia capturada.
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-web-deploy/02-02-SUMMARY.md`:

# Phase 2 Plan 2: Deploy web + verificación E2E Summary

**Web pública actualizada y primera suscripción confirmada en SES.**

## Accomplishments
## Files Created/Modified
## Decisions Made
## Issues Encountered

## Next Step

Phase 2 complete. Ready for Phase 3: iOS local bootstrap + firma.
</output>
