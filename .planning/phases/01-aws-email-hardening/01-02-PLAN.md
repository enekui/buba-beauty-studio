---
phase: 01-aws-email-hardening
plan: 02
type: execute
---

<objective>
Poner en marcha el proceso de salida del sandbox SES (review humano AWS) y dejar preparado el toggle DMARC progression + documento de warm-up para que la primera campaña real llegue a Inbox y no a Spam.

Purpose: SES en sandbox limita a 200 envíos/día SOLO a direcciones verificadas — inutilizable para clientas reales. Exit sandbox es el único bloqueante dispositivo humano restante del track email. DMARC progression (none → quarantine → reject) y warm-up planificado maximizan la deliverability en las primeras 2 semanas.

Output:
- Solicitud de producción SES enviada y capturado el Case ID para tracking.
- `aws-email/variables.tf` ya expone `dmarc_policy` como var — documentar el flip en `aws-email/WARMUP.md`.
- `aws-email/WARMUP.md` con calendario de envíos y fechas target para DMARC progression.
- Si AWS aprueba antes de que termine la sesión: verificar con `aws sesv2 get-account` que `ProductionAccessEnabled=true`.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/references/checkpoints.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-aws-email-hardening/01-01-SUMMARY.md
@aws-email/dns.tf
@aws-email/variables.tf
@aws-email/README.md

**Tech stack available:** AWS CLI v2 contra cuenta 372370374261 eu-west-1, Terraform con backend S3 ya migrado (01-01).

**Established patterns:**
- Todas las variables de infra en `aws-email/variables.tf` con validaciones.
- DMARC policy ya es una variable toggleable (`dmarc_policy` con valores permitidos `none|quarantine|reject`, default `none`).
- `aws-email/README.md` contiene el copy oficial de la justificación a AWS.

**Constraining decisions:**
- RGPD estricto → la justificación enviada a AWS debe enfatizar double opt-in + suppression automática.
- Warm-up gradual evita reputation drops (ISPs — Gmail/Outlook — aprenden reputación en 7-14 días).
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Enviar el formulario de salida del sandbox SES desde la consola AWS</action>
  <instructions>
Este es el único paso del track email que no puedo automatizar. AWS no ofrece API para disparar el Request Production Access form.

1. Login en `https://console.aws.amazon.com/sesv2/home?region=eu-west-1#/account` con la cuenta 372370374261.
2. Click en **"Request production access"** (botón arriba a la derecha).
3. Formulario — usar exactamente estos valores:
   - **Mail type**: Marketing
   - **Website URL**: https://bubabeautystudio.com
   - **Use case description** (copiar y pegar):

     > We run email marketing for Buba Beauty Studio, a beauty salon in A Coruña, Spain. All recipients opt-in via double-confirmation form on bubabeautystudio.com — a confirmation email is sent and the recipient must click the confirmation link before receiving any other emails. We use SES Contact Lists with automatic suppression for bounces and complaints, managed via SNS→Lambda. List-Unsubscribe (RFC 8058) is active on every send via ListManagementOptions. DKIM (RSA-2048), SPF, DMARC (starting p=none, progressing to quarantine after 14 clean days) are all aligned strict (adkim=s, aspf=s). Expected volume: 500–5000 emails/month initially, scaling to ~20k/month within 6 months. Complaint rate target: <0.1%. Bounce rate target: <5%.

   - **Additional contacts**: `adianny@me.com` (ya invitado como Cognito admin).
   - **Preferred contact language**: English.
4. Submit. AWS devuelve un Case ID tipo `12345678901`.
5. Anotar el Case ID.
  </instructions>
  <verification>
Tras submit, ejecutar:

```bash
aws support describe-cases --include-resolved-cases --max-results 10 --region us-east-1 \
  --query 'cases[?contains(subject, `SES`) || contains(subject, `email`)].[caseId, status, subject]' \
  --output table 2>/dev/null || echo "support API no disponible sin Business/Enterprise plan — simplemente pegar el Case ID manualmente"
```

Si Support API no responde (solo Business/Enterprise plan lo habilitan), pegar manualmente el Case ID en el resume-signal.
  </verification>
  <resume-signal>Escribir "SES request sent: CASE_ID=<id>" para continuar.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Crear WARMUP.md con calendario de envíos + DMARC progression plan</name>
  <files>aws-email/WARMUP.md</files>
  <action>
Crear un documento Markdown en `aws-email/WARMUP.md` que cubra:

1. **Calendario de warm-up (14 días)**: tabla con días 1-14, máx envíos/día, tipo (prueba interna / suscriptoras iniciales / primera campaña), indicadores de reputación a vigilar (bounce rate, complaint rate). Arrancar muy bajo (<20 envíos/día) los primeros 3 días, escalar exponencialmente.

2. **Reputación: cómo monitorear**:
   - SES Console → Account dashboard → **Reputation metrics**.
   - CloudWatch: métricas del configuration set `buba-prod-main` (Send, Delivery, Bounce, Complaint, Open, Click).
   - Suprimidos actuales: `aws sesv2 list-suppressed-destinations --region eu-west-1`.
   - Umbrales de alerta: bounce > 5% o complaint > 0.1% → parar envíos inmediatamente.

3. **DMARC progression plan**:
   - Día 0 (hoy): `p=none`. Hacer track de rua reports a `dmarc@bubabeautystudio.com`.
   - Día +14: si `aws sesv2 get-account` dice reputation OK y los rua reports no muestran fuentes no autorizadas → `terraform apply -var='dmarc_policy=quarantine'`.
   - Día +30: si estable → `terraform apply -var='dmarc_policy=reject'` (máxima protección contra spoofing).
   - Cada flip requiere ~24h de propagación DNS; planear con margen antes de una campaña grande.

4. **Qué hacer ante un bounce / complaint real**:
   - El handler Lambda `buba-prod-bounce-complaint` ya suprime automáticamente y loga en DynamoDB `buba-prod-sends-log`.
   - Revisar CloudWatch Logs del handler si hay spike: `/aws/lambda/buba-prod-bounce-complaint`.
   - Si el complaint rate cruza 0.08% → pausar campañas y revisar la lista antes de reactivar.

5. **Fechas target concretas** (calcular desde la fecha de hoy):
   - Fecha apertura ticket SES: HOY.
   - Fecha estimada aprobación AWS: HOY + 48h.
   - Fecha primera campaña piloto interna (a 5 emails propios): HOY + 3 días.
   - Fecha DMARC → quarantine: HOY + 14 días.
   - Fecha DMARC → reject: HOY + 30 días.

Usar la paleta Buba en el HTML si se quisiera convertir luego a página pública, pero es interno — solo Markdown está bien.
  </action>
  <verify>
```bash
test -f aws-email/WARMUP.md && wc -l aws-email/WARMUP.md && grep -c "## " aws-email/WARMUP.md
```
Debe existir, tener al menos 50 líneas y al menos 5 secciones (calendario, reputation, DMARC progression, bounce handling, fechas target).
  </verify>
  <done>WARMUP.md existe, cubre los 5 bloques descritos, las fechas son concretas (no "HOY + X días" literal sino calculadas).</done>
</task>

<task type="auto">
  <name>Task 3: Verificar ProductionAccessEnabled tras check-back (opcional, solo si AWS ya aprobó en la misma sesión)</name>
  <files>—</files>
  <action>
AWS SES sandbox review suele tardar 24-48h; lo normal es que este check falle al cerrar la sesión y haya que volver a correrlo al día siguiente. Intentarlo por si AWS ya respondió rápido:

```bash
aws sesv2 get-account --region eu-west-1 --query '{prod:ProductionAccessEnabled, quota:SendQuota, enforcement:EnforcementStatus}' --output table
```

Si `prod: true` → sandbox exit aprobado. Lanzar un envío de prueba contra una dirección externa NO verificada (ej. el propio `adianny@me.com` no verificado a propósito — usar una dirección que NO esté en los verified identities):

```bash
aws sesv2 send-email --region eu-west-1 \
  --from-email-address "hola@bubabeautystudio.com" \
  --destination "ToAddresses=destinatario-externo@gmail.com" \
  --content '{"Simple":{"Subject":{"Data":"Prueba post-sandbox"},"Body":{"Text":{"Data":"Esto solo debería funcionar si SES está fuera del sandbox."}}}}'
```

Si devuelve MessageId → sandbox exit confirmado. Si devuelve "MessageRejected: Email address is not verified" → todavía en sandbox, esperar y reintentar.

Si `prod: false`: registrar en el SUMMARY que el check quedó pendiente y seguir al siguiente plan — este task no es bloqueante para el resto del roadmap.
  </action>
  <verify>Output de `aws sesv2 get-account` capturado en el SUMMARY con el estado actual.</verify>
  <done>Estado de ProductionAccessEnabled registrado en el SUMMARY, con el Case ID del ticket y la fecha de próxima comprobación si aún no aprobado.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Case ID SES registrado en el SUMMARY.
- [ ] `aws-email/WARMUP.md` existe con 5 secciones y fechas concretas.
- [ ] `aws sesv2 get-account` output capturado con estado actual (prod true/false).
- [ ] Si prod=true, smoke test a dirección no verificada pasa.
</verification>

<success_criteria>
- Ticket SES enviado (humano) y tracking registrado.
- Warm-up + DMARC progression plan documentado.
- Estado actual de ProductionAccessEnabled verificado.
</success_criteria>

<output>
After completion, create `.planning/phases/01-aws-email-hardening/01-02-SUMMARY.md`:

# Phase 1 Plan 2: SES production + DMARC warm-up Summary

**Ticket exit sandbox SES enviado y warm-up plan documentado.**

## Accomplishments

- Solicitud de producción SES submiteada. Case ID: [capturar].
- `aws-email/WARMUP.md` con calendario 14-day, DMARC progression (none → quarantine día +14 → reject día +30), fechas target concretas.
- Estado actual `ProductionAccessEnabled`: [true/false al cierre de sesión].

## Files Created/Modified

- `aws-email/WARMUP.md` — nuevo, documento de referencia operativo.

## Decisions Made

- Mantener `p=none` por 14 días antes de subir a quarantine — da margen para que los rua reports lleguen y detectemos fuentes no autorizadas (ej. Booksy enviando en nombre del dominio).

## Issues Encountered

[Si AWS pide info adicional en el ticket, documentar aquí.]

## Next Step

Phase 1 complete. Ready for Phase 2: Deploy web pública con form funcional.
</output>
