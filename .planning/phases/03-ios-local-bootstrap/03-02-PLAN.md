---
phase: 03-ios-local-bootstrap
plan: 02
type: execute
---

<objective>
Activar la cuenta Apple Developer Program (99 €/año), registrar el bundle ID `com.bubabeautystudio.app`, y configurar el proyecto Xcode con el Team + capabilities Push Notifications + Background Modes.

Purpose: sin cuenta pagada no hay firma de código para TestFlight ni App Store. Sin capabilities, no hay push notifications funcionando.

Output:
- Cuenta Apple Developer Program activa y vinculada al Apple ID del negocio.
- Bundle ID `com.bubabeautystudio.app` registrado.
- Xcode project con Team asignado, signing automático, y capabilities Push + Background Modes > Remote notifications.
- `Info.plist` con `NSUserNotificationUsageDescription`.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/references/checkpoints.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ios-local-bootstrap/03-01-SUMMARY.md
@ios-app/capacitor.config.ts

**Bloqueo humano:** el pago de Apple Developer y la verificación SMS / D-U-N-S number (si es cuenta de empresa) NO se puede automatizar.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Activar Apple Developer Program</action>
  <instructions>
1. Ir a `https://developer.apple.com/programs/enroll/`.
2. Login con el Apple ID que gestionará la cuenta (preferentemente un Apple ID dedicado del negocio, no el personal).
3. Elegir tipo: **Individual** (99 €/año, más simple) o **Organization** (requiere D-U-N-S number, lleva 2-5 días). Para un salón pequeño, Individual está bien; si se quiere aparecer como "Buba Beauty Studio" y no como el nombre de la dueña en App Store, elegir Organization.
4. Completar el formulario, pagar con tarjeta.
5. Esperar email de confirmación (Individual: minutos; Organization: días).
6. Una vez activo, abrir `https://developer.apple.com/account` y anotar el **Team ID** (10 chars alphanumeric).
  </instructions>
  <verification>En `https://developer.apple.com/account` aparece membership activa.</verification>
  <resume-signal>Escribir "Apple Dev activo, Team ID=XXXXXXXXXX".</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Registrar bundle ID com.bubabeautystudio.app</action>
  <instructions>
1. `https://developer.apple.com/account/resources/identifiers/list`.
2. Botón `+` → App IDs → App → Continue.
3. Description: `Buba Beauty Studio`, Bundle ID: **Explicit** `com.bubabeautystudio.app`.
4. Capabilities — marcar:
   - `Push Notifications`
   - (dejar el resto por defecto)
5. Continue → Register.
  </instructions>
  <verification>El bundle aparece listado en Identifiers.</verification>
  <resume-signal>Escribir "bundle registrado".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Configurar Signing & Capabilities en Xcode + Info.plist</name>
  <files>ios-app/ios/App/App.xcodeproj/project.pbxproj, ios-app/ios/App/App/Info.plist</files>
  <action>
Este task combina edición de archivos (Info.plist) + configuración via Xcode UI (signing + capabilities). El `project.pbxproj` se puede editar con `xcodebuild` o con el script `xcrun`, pero lo más limpio es hacerlo vía Xcode UI y solo tocar Info.plist por CLI:

**Via CLI (automatizable):**

1. Añadir `NSUserNotificationUsageDescription` al Info.plist:

```bash
cd ios-app/ios/App/App
plutil -insert NSUserNotificationUsageDescription -string "Para avisarte de promociones y nuevos servicios del salón." Info.plist
plutil -insert UIBackgroundModes -json '["remote-notification"]' Info.plist
```

2. Setear el bundle id y display name:

```bash
plutil -replace CFBundleIdentifier -string "com.bubabeautystudio.app" Info.plist || true
plutil -replace CFBundleDisplayName -string "Buba Beauty" Info.plist
```

**Via Xcode (manual, bloquea auto):**

3. `open ios-app/ios/App/App.xcworkspace`.
4. Seleccionar target **App** → tab **Signing & Capabilities**.
5. Team: seleccionar la cuenta de Apple Developer activada.
6. Signing Certificate: Apple Development (automatic).
7. Click `+ Capability` → `Push Notifications`.
8. Click `+ Capability` → `Background Modes` → marcar "Remote notifications".

Este último bloque (Xcode UI) es inherentemente manual; no hay forma limpia de editar el `.entitlements` via CLI sin reescribir el `project.pbxproj`. Si se quiere full CLI, usar `xcodeproj` gem de Ruby — pero para primera instalación es más fiable hacerlo en Xcode.
  </action>
  <verify>
```bash
cd ios-app/ios/App/App
plutil -p Info.plist | grep -E "NSUserNotificationUsageDescription|UIBackgroundModes|CFBundleIdentifier"
test -f App.entitlements && plutil -p App.entitlements | grep -i aps-environment
```
  </verify>
  <done>Info.plist incluye NSUserNotificationUsageDescription + UIBackgroundModes con "remote-notification". `App.entitlements` incluye `aps-environment`. Team seleccionado en Xcode.</done>
</task>

</tasks>

<verification>
- [ ] Apple Developer membership activa.
- [ ] Bundle `com.bubabeautystudio.app` registrado.
- [ ] `xcodebuild -showBuildSettings -target App` muestra el Team ID correcto.
- [ ] Push Notifications + Background Modes capabilities visibles en Xcode.
</verification>

<success_criteria>
- Apple Dev pagado y confirmado.
- Bundle ID registrado.
- Proyecto Xcode firmable (no signing errors).
</success_criteria>

<output>
After completion, create `.planning/phases/03-ios-local-bootstrap/03-02-SUMMARY.md`.

## Next Step

Ready for 03-03-PLAN.md: OneSignal + APNs key + build firmada.
</output>
